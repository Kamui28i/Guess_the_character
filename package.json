{
  "name": "guess-the-culprit",
  "version": "1.0.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm --prefix guess-the-culprit install --include=dev && npm --prefix guess-the-culprit run build",
    "start": "npm --prefix guess-the-culprit start"
  }
}
