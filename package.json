{
  "name": "guess-the-culprit",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm --prefix guess-the-culprit install && npm --prefix guess-the-culprit run build",
    "start": "npm --prefix guess-the-culprit start"
  }
}
